pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        sh 'mvn clean compile package'
      }
    }
    stage ('login') {
      steps {

      	sh 'docker-compose up -d'

        sh 'echo login successful'
			}

      }
    }
  }
}